using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PSK_Test_Script_Grape : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Grape!!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
